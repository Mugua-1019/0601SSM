<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hospital.mapper.MedicineMapper">

    <select id="findAll" resultType="Medicine">
        SELECT id, name, specification, stock, price
        FROM medicines
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="Medicine">
        SELECT id, name, specification, stock, price
        FROM medicines
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Medicine">
        INSERT INTO medicines (name, specification, stock, price)
        VALUES (#{name}, #{specification}, #{stock}, #{price})
    </insert>

    <update id="update" parameterType="Medicine">
        UPDATE medicines
        SET name = #{name},
            specification = #{specification},
            stock = #{stock},
            price = #{price}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM medicines
        WHERE id = #{id}
    </delete>
</mapper>
