<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hospital.mapper.DoctorMapper">

    <select id="findAll" resultType="Doctor">
        SELECT id, name, department, title, phone, email
        FROM doctors
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="Doctor">
        SELECT id, name, department, title, phone, email
        FROM doctors
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Doctor">
        INSERT INTO doctors (name, department, title, phone, email)
        VALUES (#{name}, #{department}, #{title}, #{phone}, #{email})
    </insert>

    <update id="update" parameterType="Doctor">
        UPDATE doctors
        SET name = #{name},
            department = #{department},
            title = #{title},
            phone = #{phone},
            email = #{email}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM doctors
        WHERE id = #{id}
    </delete>
</mapper>
